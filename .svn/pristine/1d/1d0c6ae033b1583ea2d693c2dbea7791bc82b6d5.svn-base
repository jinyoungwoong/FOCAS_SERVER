namespace NCManagementSystem.Libraries.Controls.TextBox
{
    public class TextBoxConstsDefiner
    {
        public enum TextBoxStyles
        {
            Default,
            Numeric,
            Decimal,
            Alphabet,
            EMail,
            ExceptHangul
        }

        internal struct MessageSet
        {
            public const string REQUIRED_INPUT = "Input is required.";
            public const string VALID_NUMBER = "Only valid for a number.";
            public const string VALID_DECIMAL = "Only valid for a decimal format.";
            public const string VALID_ALPHABET = "Only valid for a alphabet.";
            public const string VALID_EMAIL = "Only valid for E-Mail format.";
            public const string VALID_IPADR = "Only valid for IP address format.";
            public const string VALID_NUMBER_AND_DOT = "Only valid for number and dot.";
            public const string VALID_NUMBER_AND_ALPHABET = "Only valid for a number and alphabet.";
            public const string INVALID_LESS_MININUM = "Less than the minimum length limit.";
            public const string INVALID_EXCEEDED_MAXIMUM = "Maximum length limit has been exceeded.";
            public const string INVALID_MINUS_POSITION = "'-' can be at start position only.";
            public const string INVALID_DOT_POSITION = "'.' can't be at start position.";
            public const string INVALID_DOT_COUNT = "Decimal can't have more than one dot.";
            public const string INVALID_DOT_POST_EXCEEDED = "The dot post length limit has been exceeded.";
        }

        internal struct ExtensionsPropertyNames
        {
            internal enum ErrorProviderProperties
            {
                ErrorBlinkStyle,
                ErrorIconAlignment,
                ErrorProviderMargin
            }

            internal enum WaterMarkProperties
            {
                WaterMarkFont,
                WaterMarkTextAlignment,
                WaterMarkText,
                WaterMarkForeColor
            }

            internal enum DecimalStyleProperties
            {
                DotPostLength,
                IsFloatingPoint,
                IsCommaSeparator
            }
        }
    }
}
